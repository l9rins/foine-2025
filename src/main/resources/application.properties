server.port=8080

# ===============================
# Database Configuration (Railway PostgreSQL)
# ===============================

# Use Railway DATABASE_URL environment variable with jdbc: prefix
spring.datasource.url=jdbc:${DATABASE_URL:jdbc:postgresql://localhost:5432/foine_db}

# 2. Driver Configuration
spring.datasource.driver-class-name=org.postgresql.Driver

# 3. Hibernate Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# 4. Connection Pooling (Hikari) - optimized for Railway
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Cloudinary Configuration
cloudinary.cloud_name=dajqlsxlz
cloudinary.api_key=878257588782165
cloudinary.api_secret=jWLXqxHxb6EEm_enjxaNobZMXU4

# JWT
jwt.secret=mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong123456789
jwt.expiration-ms=86400000

# Multipart
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.max-request-size=10MB
